#!/usr/bin/env ruby

=begin
  require 'htmler'
rescue LoadError
  require 'rubygems'
  require 'htmler'
=end

require './lib/htmler'

htmler = Htmler.new
htmler.compile_file(ARGV[0])
puts htmler.to_html